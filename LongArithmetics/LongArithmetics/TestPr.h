#pragma once

void task();

char* sum(char* number1, char* number2);

// Если равны, то вернем 0, если первое больше, то 1, в противном случае -1.
int equalByMod(char* number1, char* number2);

char* substraction(char* number1, char* number2);